class Ball:
    def __init__(self, ball_id, x, y, r, x_speed, y_speed, color="red"):
        self.id = ball_id
        self.x = x
        self.y = y
        self.r = r
        self.color = color
        self.x_speed = x_speed
        self.y_speed = y_speed
